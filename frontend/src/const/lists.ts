import { InterviewStatus, InterviewType } from "../services/interviewsApi";

export const interviewStatuses = Object.values(InterviewStatus);

export const interviewTypes = Object.values(InterviewType);
